[pytest]
testpaths = tests
# Live (real-LLM) tests are excluded by default to keep the default run fast and
# token-free. Opt in with:  pytest -m live
addopts = -m "not live"
markers =
    live: end-to-end tests that call the real configured LLM (skipped without OPENAI_API_KEY)
